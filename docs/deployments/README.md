# Deployment guidance

Environment-specific deployment histories are maintained in the private operator
Wiki. Public deployment configuration and checks are in `deployment/` and
`docs/reference/server/`. Use your own service hostname in configuration.
